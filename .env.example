# Auth0 Server Configuration

# JWT/JWE Secret Key (required)
# Generate a secure random key for production: openssl rand -hex 32
JWE_SECRET=your-secure-secret-key-here

# Server Configuration
SERVER_ADDRESS=:8080
ISSUER=auth0-server
DOMAIN=localhost:8080

# Database Configuration
# Use "memory" for testing without PostgreSQL, "postgres" for production
DB_DRIVER=memory
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-database-password
DB_NAME=Auth0_DB
DB_SSL_MODE=disable
